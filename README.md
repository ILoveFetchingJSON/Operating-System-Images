# Operating-System-Images
If you want to see some of my bootloader and kernel projects, go here!
I created a image.img file, which is a boot-to-floppy-disk bootable image which is a bootloader. You can run it with qemu with:
qemu-system-x86_64 -fda image.img

If you have Windows, install WSL as this is only a Linux command.
